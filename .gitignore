# Bytecode
__pycache__/
*.py[cod]
*$py.class

# Virtual env (uv 기본 위치)
.venv/
venv/
env/

# uv 캐시
# (uv.lock, .python-version 은 커밋)

# Test / lint cache
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Packaging
*.egg-info/
build/
dist/
*.egg

# Env
.env
.env.*
!.env.example

# 학습 모델 산출물 (git에 두기엔 큼)
*.pkl
*.joblib
*.h5
*.pt
*.onnx
# app/db/models 는 소스 코드 — ML 산출물은 루트 /models/ 만 무시
/models/

# 대용량 시드 데이터 (개별 다운로드)
seed/smartfarm_top.csv
seed/kamis_history.csv

# Alembic / SQLite 임시
*.db-journal
*.sqlite3

# SQLite 런타임 DB는 제외(앱 시작 시 자동 생성, 사용자별 데이터).
# data/embeddings·data/farminfo·data/weekfarm(RAG 임베딩/정리 데이터)는
# 팀 공유를 위해 커밋한다 — 재생성에 농사로 키+OpenAI 비용+시간이 들기 때문.
*.db
*.db-wal
*.db-shm

# 수확 인증 사용자 업로드 사진 (커밋 금지)
data/uploads/
